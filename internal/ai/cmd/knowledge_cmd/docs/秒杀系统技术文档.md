# 高并发秒杀系统重构记录 (2026.03.26)

基于**Go+Gin+GORM+MySql+JWT+Redis+RabbitMQ+Docker**的电商后端系统，实现了完整的购物流程。

## 技术栈
- Go + Gin框架
- MySQL + GORM
- JWT认证
- API-Service-DAO分层架构
- Redis+Lua脚本 
- RabbitMQ
- Docker

## 功能模块
- ✅ 用户认证 (JWT)
- ✅ 商品管理 (列表/搜索/详情)
- ✅ 购物车 (添加/查看/删除)
- ✅ 订单系统 (下单/列表/详情)

## 技术架构

### 分层架构设计
- **API层**: HTTP请求处理
- **Service层**: 业务逻辑实现  
- **DAO层**: 数据库操作封装
- **Domain层**: 数据模型定义

### 错误处理标准化
``` go
type BusinessError struct {
    Code int    `json:"code"`
    Msg  string `json:"msg"`
}
```

## 性能压测结果

### 2026.03.14 - 商品列表接口（读操作）
- **工具**: hey
- **测试环境**: 本地开发环境（Windows 11）
- **并发**: 50
- **时长**: 30秒
- **QPS**: 10,196 请求/秒
- **平均延迟**: 4.9ms
- **错误率**: 0%
- **备注**: 纯MySQL版本，读操作性能优秀

### 2026.03.14 - 订单创建接口（写操作）
- **工具**: hey
- **测试环境**: 本地开发环境（Windows 11）
- **并发**: 100
- **时长**: 30秒
- **QPS**: 完全失败
- **平均延迟**: 超时/失败
- **错误率**: 100%
- **备注**: 写操作存在严重并发冲突，需要优化（3.14）；加入购物车和下单接口应该是绑定的，这里单独测试下单接口有点蠢了。（3.18）

### 2026.03.18 - 订单创建接口（加入购物车+下单）
- **工具**: 自定义Go压测脚本
- **测试环境**: 本地开发环境（Windows 11）
- **用户数**: 500个并发用户
- **操作数**: 每个用户5000次操作
- **测试时长**: 60秒
- **QPS**: 1,748.63
- **成功率**: 97.33%
- **平均延迟**: < 100ms
- **备注**: 在100个并发用户的情况下，能达到100%的成功率，1500的QPS

### 2026.03.24 - 集成RabbitMQ实现异步下单与削峰填谷
- **工具**: 自定义Go压测脚本
- **测试环境**: 本地开发环境（Windows 11）
- **总请求数**: 160427
- **成功订单**: 155383
- **失败订单**: 5044
- **测试时长**: 1m0.5169341s
- **QPS**: 2,650.94
- **成功率**: 96.86%
- **备注**: 性能提升约50%

### 2026.03.26 - 异步架构性能摸底 (单实例 & 单核资源限制)

为了模拟线上真实、资源受限的环境，所有测试均在同一个配置下进行：**单个`app`实例，并限制其最多使用1个CPU核心**。通过逐步增加总请求数，我们得到了系统在不同压力等级下的性能表现数据（没有取消日志，可能导致了少量性能下滑）：

| 总请求数 | QPS      | 成功率 | 备注 |
| :---     | :---     | :---   | :--- |
| 1,000    | 1113.77  | 98.10% | 瞬时脉冲，峰值初现 |
| 10,000   | ~979-1001 | ~98.15-98.30% | 短时高压，稳定峰值 |
| 25,000   | ~558-597 | ~92.30-97.18% | **性能拐点**，QPS与成功率双双下跌 |
| 50,000   | ~617.67-669.62 | ~92.07-97.29% | 负载加倍，性能意外回升 |
| 100,000  | 589.38   | 93.52% | 持续重压，巡航速度确立 |

**数据分析与结论:**

1.  **峰值性能与性能拐点**: 在低负载（1k-10k请求）下，系统展现出卓越的爆发力，峰值QPS可达 **~979-1114**。然而，当负载攀升至2.5万请求时，系统遭遇明显的 **"性能拐点"**：QPS骤降至 **~558-597** 区间，且成功率开始波动，这标志着系统处理能力已达上限。

2.  **瓶颈分析与意外现象**: 性能拐点处的表现，结合10万请求重压下的数据，清晰地将瓶颈指向 **MySQL的写入性能**。高并发下的数据库 **锁竞争** 是导致事务失败和吞吐量下降的根本原因。值得注意的是，在5万请求量时观察到的"性能回升"现象（QPS回升至**666**，成功率高达**97.29%**），这可能源于测试间隙的资源缓冲或GC等偶发因素，虽为异常点，但更凸显了数据库瓶颈在高压下的不稳定性。

3.  **性能基准与优化方向**: 本轮测试为单核资源下的系统确立了关键性能基准：**持续巡航QPS稳定在600左右**。这个核心指标量化了系统的稳态处理能力。基于以上分析，下一步的优化方向已十分明确：**必须集中资源解决数据库层的写入瓶颈**，提升系统在高并发场景下的稳定性和成功率。

## 经验总结与吐槽 💡
- **分层架构的思想很重要**，现在重构的项目就是从这个思路出发的，由**api层、service层、dao层、domain层**组成。大一下写这个项目的时候，把这四层揉到一起了，业务逻辑极其混乱，导致我在初期重读代码的时候特别头疼。后面废了大力气，把业务逻辑拆分成不同的层，代码结构才变得清晰。（2026年3月14日）
---
-   最开始在写登录接口的时候，**请求和响应**没有**分开处理**，也就是说，我前端传来请求，我直接bind到user结构体，而不是registerRequest。这导致了个什么后果呢？这导致我返回响应时，把user的password也返回了。这是个严重的安全漏洞。于是我就推倒重写，为**请求和响应分别设计结构体**，这样就解决了传入和返回不必要的字段的问题。我觉得这个思路太巧妙了哈哈哈。（2026年3月14日）
---
- 我最开始写的时候没有注意**错误处理**，又是 errors.New 的，又是 fmt.Errorf 的，格式特别混乱。而且很多时候错误处理形同虚设，就是报错了我也找不出是哪里的问题。后来我就设计了一个BusinessError结构体，用来统一错误处理，然后const了很多错误码，这样返回的错误既清晰又统一，也方便后续维护和拓展，感觉这个设计很不错。（2026年3月14日）
---
- 大一写项目的时候，根本就不懂**原子函数**，把所有业务逻辑都写到一起，比如说service层的login函数，里面包含了用户验证、token生成、数据库操作等多个步骤（也如第一点吐槽说的，api，service，dao层逻辑叠到一个函数写，你敢信吗，我api层直接调用dao层函数，写的简直是狗屎），这导致了大量的代码耦合，也不利于后续的维护和拓展。现在重构项目，我就把业务逻辑拆分成小函数，每个函数只负责一件事，这样就提高了代码的可维护性和复用性。有那么一小会儿，觉得自己像架构师哈哈哈。（2026年3月14日）
---
- 这次重构项目的时候，我写了大量的**通用函数**，比如通用插入，通用查询，通用删除等等。之前学概念的时候，对**泛型**的理解一直不太深刻。但这次写通用函数，就发现泛型的优势很大。比如我写一个通用的插入函数，我可以传入任意结构体，它就会根据结构体的字段，自动生成对应的SQL语句。这就避免了我每次写插入函数都要写一遍SQL语句的问题，也提高了代码的复用性。（2026年3月14日）
---
- **代码命名一定一定要规范！**，变量名、函数名、结构体名等都要符合命名规范。我大一写的时候，那几个ID（userID，productID等），全部没做区分，直接用ID来表示，导致代码可读性非常糟糕，看半天才理解业务逻辑。现在重构项目的时候，就对这部分做了优化。（2026年3月14日）
---
- 写注释真是个好习惯啊。大一写的代码，有部分注释了有部分没有。我读代码的时候，明显感觉到有注释的好读得多，即使过了一年时间，也能快速上手。相对的，那些没注释过的代码（加上我命名也不规范），读起来就会头疼很多。（2026年3月14日）
---
- **一定一定要测试**!当初大一写的代码，我现在测试根本跑不通，那个时候觉得不报错就是对的，现在看来荒唐无比。这次重构项目就是完成一个板块，就测试一个板块。比如我写完商户接口，就直接测试商户接口，然后再写其他接口，保证之前的接口不会出错。有好多问题goland是根本不会报错的，甚至服务端也能200，但是业务逻辑就是有问题。我印象最深的就是，商品重名，会给商户返回未知错误（正常情况会返回商品重名的友好提示）。这种问题不在apifox上测试，根本不可能发现，这个错误的原因是，我有个地方忘记包装bizErr了，是直接返回的原生err，导致我api有个区分业务错误和原生错误的错误鉴定函数错误判断，把商品名重复当成原生错误，最后生成了个未知错误返回。所以测试真的是重中之重。（2026年3月14日）
---
- 今天集成了**Redis**，缓存了商品详情，用lua脚本实现了对购物车及订单缓存的更新和删除，也是完成了高并发的搭建。这此压测500并发能有97%的正确率，1700+的QPS，感觉还算不错，没有辜负这两天的努力。（2026年3月18日）
--- 
- 最开始我**设计缓存**的时候，我直接缓存的商品详情，没有单独去缓存商品库存，而我在下单逻辑中，是直接对商品详情中的库存进行减扣，这导致了一个问题：高频变化的库存和低频变化的商品详情混在一起，更新时有些不必要的开销，而且还有数据不一致的风险。最后在ai的帮助下，我把库存独立缓存了出来，并通过lua脚本来原子减扣库存，才解决了刚刚的问题。我觉得这个思路很不错，把高频信息和低频信息分开处理，避免了很多麻烦。（2026年3月18日）
---
- **压测脚本**是我自己设计，然后让ai帮我跑的。我设计的时候犯蠢了，想着用一个用户，来完成高并发秒杀测试。我那会儿想着，反正就是调接口嘛，一个和一百个没什么区别。先去看起来还是太愚蠢了，白白花了几个小时去调压测脚本。错误的原因很简单，一个人做不到在把商品加入购物车的同时，点击下单。这是两个串行的动作，但是在设计的时候却按照并行设计。这就导致了存在一种可能：先下单后加入购物车。于是乎商品加入购物车成功，但是下单失败（购物车为空）。后来花了很久排除错误，才突然意识到，怎么可能用一个用户来模拟并发？现在也觉得蠢得没变......（2026年3月18日）
---
- 最开始的时候**QPS**只有十几，我就非常地纳闷，为啥QPS才这么点？后来我看了我log的日志，发现很多请求有几百ms，甚至上千ms，而大多数只有几ms。我仔细读信息才意识到，我有的接口没有集成redis。一开始我只为订单功能加了redis，毕竟这个比较明显，秒杀那肯定跟下单有关啊。后面看见了这种高延迟的响应，才猛地反应过来，我没有给购物车接入redis，导致每次读取购物车商品数据，都要访问数据库。再次意识到了日志的重要性。感觉自己写的后端代码，一大部分都是跟错误处理和日志有关。（2026年3月8日）
---
- 写压测脚本的时候，好像ai帮我调整过**连接池**。最开始压测的时候，结果不是很理想，听ai的建议检查并修改连接池后，QPS有了明显的提示。不过现在我对连接池一知半解的，后续再深入了解吧。（2026年3月18日）
---
- 还没接**lua脚本**的时候，有点畏难心理，怕自己学不会。后来慢慢接触，还是学会了简单的lua脚本。这让我想起了大一学习markdown语法的时候，也是觉得很难，结果最后轻松掌握。可能学习还是得迎难而上吧，学着学着就不难了哈哈。（2026年3月18日）
---
- 目前项目只解决了缓存穿透的问题，我缓存了空值来放在缓存穿透。对于缓存击穿，我只做了简单的预热，就是在启动的时候自动缓存商品详情，尽量避免商品缓存过期。不过也确实不可能每天都重启服务，所以这里还有一点缺陷。而缓存雪崩，我没有做任何处理。不过我有个思路是设计随机的过期时间，来防止缓存同时失效。这部分确实还得深入一下，等后面集成了MQ再来优化吧。（2026年3月18日）
---
- 最开始我设计预热的思路，还不是现在这种预热所有热点商品，我选择的**懒加载**，因为我想着，用户查询这个商品说明，说明这个商品比较热门，就缓存到数据库中，不查就不存，减小开销。正常情况下这样确实没问题，但是如果在秒杀场景下，这就很容易出错。秒杀活动开始那瞬间，一万个人同时查询这个热门商品，这个商品是没有缓存的，请求直接打到数据库，数据库很可能就挂了。后来就选择了启动服务时加载所有商品。其实我本来像设计成加载热点商品的，但是考虑到热点商品算法等问题，我暂时没有添加。毕竟这是个高并发项目，还是要聚焦自己的核心功能的。（2026年3月18日）
---
- 写完reademe已经凌晨五点了，已经听见公鸡打鸣了。希望后面接入MQ和部署docker也能顺顺利利吧。（2026年3月18日）
---
- 项目也终于是要接近尾声了，我也基本**完成了所有的功能模块**。可能有一段时间不会写新功能了，准备去投实习了。今天改了不少bug，原来的service启动逻辑都是错的，不知道那时候是怎么跑成功的。而且最开始忘记修改数据库了，每次库存减扣只用lua减了redis的库存，MySQL都没有更改。这个bug太离谱了。后续引入了事务锁，不可避免的导致了性能下降。主要原因还是加入了一个长事务，后面有时间再去把它优化一下吧。（2026年3月26日）
---
- **压测**了好几次，结果都不太理想，那个时候没有限制cpu，但是只有几百的QPS。最后发现不是项目的问题，是压测脚本的问题，并发开太高了，自己的电脑扛不住。我看我后台cpu占用五十多。只能说长个教训吧，以后ai跑脚本自己得多注意一下，再怎么也得过目。（2026年3月26号）
---
- 除了网络问题，**docker**的配置意外的顺利，pull镜像之后，直接运行容器，就可以正常访问了，好像没出什么怪毛病，不知道该说幸运还是实力哈哈哈。（2026年3月26号）
---
- 高并发的情况下排查问题太困难了，日志一发就是一大堆，改起来特别头疼，这次是对着日志一点一点改的，浪费了不少时间，希望以后能找到好点的办法吧。（2026年3月26号）
---
- 今天从十点写到两点，除了吃饭就一直在敲代码，我好像也没干什么啊，怎么时间过得这么快...（2026年3月26号） 

## 后续优化
- [x] Redis 集成
- [x] Docker 部署
- [x] MQ 消峰
- [ ] 性能优化
